export const styles = (theme) => {
    const {
        miscellaneous: { spacing }
    } = theme;
    return {
        subtitle: {
            display: 'none',
            marginBottom: spacing * 1
        }
    };
};
